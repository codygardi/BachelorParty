# Robbie's Bachelor Party RSVP

Standalone RSVP website for Robbie Merchant's bachelor party.

Open `index.html` locally, or publish this repository with GitHub Pages. Shared RSVP and availability persistence is handled by the Cloudflare Worker configured in `bachelor-party-config.js`.
